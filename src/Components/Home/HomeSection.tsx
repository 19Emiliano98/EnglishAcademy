import { AppBar } from '../Assets/AppBar.tsx';
import { Box } from '@mui/material';

const HomeSection = () => {
  return (
    <Box sx={{ m: -1 }}>
      <AppBar />
    </Box>
  )
}

export default HomeSection